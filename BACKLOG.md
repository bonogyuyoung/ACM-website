# BACKLOG — 자동 실행 큐 (주 2회)

자동화의 단일 지시서. 배경은 `PLAN.md`, 사람이 풀 것은 `BLOCKERS.md`, 주제 아이디어는 `IDEAS.md`(기준 데이터 아님).

## 실행 알고리즘
```
1. BACKLOG.md · BLOCKERS.md 를 읽는다
2. loop:
     선행 조건이 모두 [x] 이고 게이트가 열린 작업 중 첫 번째를 고른다
     없으면 → CHANGELOG 기록 후 종료
     구현한다
     └ 성공 → [x] 표시, 커밋
     └ 막힘 → BLOCKERS.md 에 "무엇이 필요한지" 추가, 작업에 (blocked) 표시,
              커밋하지 않고 loop 계속   ← 여기서 실행을 끝내지 않는다
3. CHANGELOG.md 에 한 것과 새로 막힌 것을 기록
```

## 금지 사항 (어기면 실패로 간주하고 되돌린다)
- **콘텐츠 하드코딩 금지.** 에피소드 제목·시즌 이름·팀원 이름·과목명을 코드에 쓰지 않는다. 예시 데이터도 `sample` 표시 없이 넣지 않는다.
- **개수 가정 금지.** 0개에서도 100개에서도 레이아웃과 로직이 성립해야 한다.
- **표시 명칭은 설정값에서 읽는다.** '시즌/에피소드'를 코드에 문자열로 박지 않는다.
- 빌드 도구·프레임워크·번들러·npm 도입 금지
- 기존 HTML 7개 파일명 변경 금지
- 공통 UI 복붙 금지 — `script.js` 렌더 함수로 추가
- **RLS 켜기 전 Supabase 테이블 생성 금지**
- 게이트 통과 전 개인정보 서버 저장 금지
- 본문·강의·문항 내용 작성 금지 — 그릇과 빈 필드까지만
- 한 커밋에서 구조 변경 파일 3개 이상 금지

## 파일명에 대한 메모
표시 명칭은 설정값이지만 **파일명은 기본값으로 고정한다** (`seasons.html`, `episode.html` 등). URL이 자주 바뀌면 외부 링크가 끊긴다. 화면에 보이는 단어만 설정에서 온다.

---

# 1단계 · 껍데기 — 콘텐츠 0개에서 도는 사이트

### [x] A1 — 저장소 구조 정리 + 배포 확인
선행: 없음 · 파일: `README.md`, `.nojekyll`
완료: Pages 주소에서 7개 페이지가 전부 열리고 상대경로 링크가 살아 있음. README에 폴더 구조와 편집 안내.

### [x] A2 — 빈 계층 스키마 + 설정 분리
선행: A1 · 파일: `data.js` (단독)
완료: `config`(사이트명·표시 명칭·발행 주기·상태 단계 목록·과목 분류)와 `collections`(묶음 → 항목 2계층, **비어 있음**)로 재구성. 각 항목은 `article`/`video`/`paper` 슬롯을 선택적으로 가짐.
**금지: 시즌·에피소드를 하나라도 시딩하지 말 것.** 기존 Antibiotic Resistance / Vaccines / CRISPR 3건도 제거.
완료: `topics`/`articles`/`videos`를 `collections`에서 파생시켜 `script.js` 무수정으로 기존 페이지 동작.

### [x] A3 — 상태 파이프라인
선행: A2 · 파일: `styles.css`, `script.js`
완료: 상태 단계를 `config`에서 읽어 배지를 생성(단계 목록 자체가 설정값). 기본값 `planned → researching → drafting → review → published`. `getBadgeClass()`가 모르는 상태를 받으면 `.badge.default`로 떨어지도록 수정 — 현재는 배경색 없는 배지가 나오는 버그.

### [x] A4 — 빈 상태 시스템
선행: A3 · 파일: `styles.css`, `script.js`
완료: 목록이 0개일 때의 안내 블록을 공통 컴포넌트로. **깨진 화면이 아니라 의도된 화면으로 보여야 한다.** 모든 목록 렌더 함수가 이걸 쓴다. 이 작업 이후 사이트는 콘텐츠 없이도 완성된 인상을 준다.

### [x] A5 — 디자인 토큰 + 컴포넌트 규약
선행: A4 · 파일: `styles.css`, `README.md`
완료: 색·타이포·간격을 `:root` 스케일로 정리. 카드/배지/버튼/빈상태 클래스 규약을 README에 문서화.

### [x] A6 — 묶음 목록 페이지
선행: A5 · 파일: `seasons.html`, `script.js`
완료: 묶음 카드 목록(제목·부제·항목 수·진행률). 0개면 A4의 빈 상태. Explore 메뉴에 추가.

### [x] A7 — 항목 상세 페이지
선행: A6 · 파일: `episode.html`, `script.js`
완료: `episode.html?id=` 하나로 모든 항목 렌더. 핵심 개념 목록, 큰 질문, 상태 배지, 연결된 아티클/영상/논문 자리. 잘못된 id는 목록으로 안내.

### [x] A8 — 아티클 상세
선행: A7 · 파일: `article.html`, `data.js`
완료: `article-template.html`의 7섹션(요약·왜 중요한가·핵심 용어·본문·단계별 설명·면책·출처)을 데이터 필드로 승격. 빈 필드는 "작성 중"으로 표시되고 레이아웃 유지. 면책 문구는 기존 것 그대로. **본문은 비워 둔다.**

### [x] A9 — 영상 임베드
선행: A7 · 파일: `script.js`, `styles.css`
완료: `youtubeId` + 16:9 반응형 임베드. 없으면 Coming Soon. `featuredVideo` 하드코딩 제거하고 "가장 최근 published" 자동 선택 — 없으면 섹션 자체를 숨김.

### [x] A10 — 홈 재구성
선행: A9 · 파일: `index.html`, `script.js`
완료: 프로젝트명·미션·짧은 설명·현황을 전부 `config`와 `collections`에서 계산. 하드코딩된 숫자 0개. 콘텐츠가 없으면 "준비 중" 상태로 자연스럽게 보임.

---

# 2단계 · 콘솔 ★ — 여기부터 코드를 안 건드린다

### [ ] B1 — Supabase 연결 + 스키마 + RLS
선행: A2 · **게이트: Supabase 프로젝트 생성 (X2)**
완료: 테이블 설계(config, collections, items, articles, videos, papers, lessons, questions, staff). **모든 테이블 RLS 활성화** — 공개 읽기 / 스태프만 쓰기. publishable 키는 클라이언트에 두되 RLS로 방어.

### [ ] B2 — 콘텐츠 DB 이관
선행: B1, A10 · 완료: 사이트가 DB에서 읽음. `data.js`는 DB 실패 시 fallback으로 유지(오프라인·장애 대비). 개인정보 0.

### [ ] B3 — 스태프 로그인
선행: B2 · 완료: Supabase Auth 이메일 로그인, 허용 목록에 있는 사람만. 일반 이용자 가입 경로는 아직 없음.

### [ ] B4 — 콘솔: 묶음·항목 관리
선행: B3 · 완료: 묶음과 항목을 **화면에서 만들고 수정하고 지우고 순서를 바꾼다.** 상태 변경 포함. 이 작업이 끝나면 콘텐츠 계획이 바뀌어도 저장소를 건드릴 일이 없다.

### [ ] B5 — 콘솔: 아티클·논문·파일
선행: B4 · 완료: 아티클 7섹션 편집, 논문 PDF·썸네일 업로드(Supabase Storage), 영상 링크 등록.

### [ ] B6 — 콘솔: 사이트 설정
선행: B4 · 완료: 사이트명, 표시 명칭('시즌/에피소드' 등), 발행 주기, 상태 단계 목록, 과목 분류, 팀 명단·역할을 **화면에서 편집.** 문서의 팀 명단과 발행 규칙은 오래된 것이므로 초기값을 넣지 않는다.

---

# 3단계 · 기능

### [ ] C1 — 검색 + 과목 필터
선행: B4 · 완료: 제목·핵심 개념·큰 질문 대상 클라이언트 사이드 검색. 과목 필터는 `config`의 분류를 읽음. 외부 라이브러리 없이.

### [ ] C2 — 과목별 랜딩
선행: C1 · 완료: 과목 하나당 페이지. 과목 목록이 콘솔에서 바뀌면 페이지도 따라 바뀜(정적 파일 추가 금지, 쿼리 파라미터 방식).

### [ ] C3 — papers.html
선행: B5 · 완료: 연구 논문 목록 + PDF 링크 + 인용 형식. 아티클과 별도 축.

### [ ] C4 — 아카이브
선행: A10 · 완료: 월별 발행 연대기. 발행 주기는 `config`에서 읽어 목표선을 표시(고정값 금지).

### [ ] C5 — 관련 콘텐츠 추천
선행: C1 · 완료: 핵심 개념 교집합 기반 규칙 추천(모델 사용 안 함). 항목 하단에 3개, 부족하면 있는 만큼만.

### [ ] C6 — future.html 3부 구조
선행: A10 · 완료: 장기 플랫폼 3부 — ① 과학 대중화 ② 학업 코스 ③ 개인화 학습. ③은 현재 상태(게이트 대기)를 명시.

### [ ] C7 — about.html
선행: B6 · 완료: 팀·역할을 **콘솔 데이터에서** 렌더. 문서의 명단은 오래됐으므로 **하드코딩 금지**, 비어 있으면 빈 상태로 표시.

### [ ] C8 — contact.html 참여 안내
선행: C7 · 완료: 문의·주제 제안·참여 신청 경로. 개인정보는 수집하지 않고 외부 폼으로 연결.

### [ ] D1 — courses.html (mix version)
선행: C2 · 완료: 문서가 지정한 혼합 레이아웃 — Khan Academy식 과목별 템플릿 + Pearson식 하단 과목 선택 바. 3분류(대중화 영상 / 강의 / AP) 진입점.

### [ ] D2 — 강의 코스 구조
선행: D1 · 완료: 코스→단원→강의 3계층 스키마와 목록. 콘솔에서 생성·편집 가능. **내용은 비움 (X5)**

### [ ] D3 — AP 코스 구조
선행: D2 · 완료: AP 과목 코스 껍데기. **담당 스태프 자격 규정은 미확정이므로 표시하지 않는다** — 확정되면 `config`에 넣어 표시.

### [ ] D4 — lesson.html
선행: D2 · 완료: 강의 상세 — 영상 + 노트 + 첨부 + 다음 강의. 빈 강의도 레이아웃 유지.

### [ ] D5 — 연습문제 엔진
선행: D4 · 완료: 문항 스키마(객관식·단답·해설·과목 태그·난이도), 즉시 채점, 해설 표시. 전부 클라이언트 채점. 콘솔에서 문항 등록. **문항 내용은 비움 (X5)**

### [ ] D6 — 오답 노트
선행: D5, E1 · 완료: 틀린 문항 모아보기와 재시도. `localStorage`만.

### [ ] D7 — 코스 진도
선행: D6 · 완료: 코스별 진도바와 '이어서 학습'. 로그인 없이 동작.

### [ ] E1 — 영상 진도 저장
선행: A9 · 완료: 시청 위치를 `localStorage`에 저장하고 이어보기.

### [ ] E2 — 북마크
선행: E1 · 완료: 항목·아티클·강의 저장하기와 저장 목록.

### [ ] E3 — 학습 기록 대시보드
선행: E2 · 완료: 본 영상·읽은 아티클·푼 문제를 한 화면에. 전부 로컬 데이터에서 계산.

### [ ] E4 — 기록 내보내기·가져오기
선행: E3 · 완료: JSON으로 내보내고 다른 기기에서 불러오기. 계정 게이트가 길어져도 이용자가 손해 보지 않게 하는 장치.

### [ ] E5 — 설정 실동작
선행: A5 · 완료: 다크/라이트 + 색 테마 + 글자 크기. 현재 Settings 패널은 "Coming soon" 텍스트뿐. `localStorage`만.

### [ ] F1 — 반응형 기반
선행: A5 · 완료: 모바일 우선 그리드. 360px 폭에서 가로 스크롤 없음.

### [ ] F2 — 접근성 기준선
선행: F1 · 완료: 본문 대비 4.5:1 이상, 드롭다운 키보드 조작, 모든 이미지 alt, landmark 태그, 보이는 focus ring.

### [ ] F3 — SEO·공유
선행: F1 · 완료: 페이지별 meta description, OG 태그, sitemap, favicon.

### [ ] F4 — 발행 캘린더
선행: C4, B6 · 완료: 콘솔에서 발행 리듬 추적. 목표 주기는 `config`에서 읽음. 목표 미달이면 콘솔에 경고.

---

# 4단계 · 게이트 뒤

자동화는 **연결 직전까지** 만들어 두고 멈춘다. 게이트가 열리면 연결만 하면 켜진다.

### [ ] G1 — 개인정보처리방침·약관 초안 + 동의 UI
선행: B3 · 완료: 수집 항목·보관 기간·삭제 요청 절차 초안과 동의 화면을 **비활성 상태로** 배치. 이 문서 자체가 X4를 푸는 재료가 된다.

### [ ] G2 — 이용자 계정 화면 일습
선행: G1 · 완료: 가입·로그인·비밀번호 재설정·프로필 화면과 검증·에러 처리까지. **Supabase 연결은 비움.**

### [ ] G3 — 계정 활성화
선행: G2 · **게이트: 지도교사 승인 + 보호자 동의 양식 (X4)**
완료: G2를 실제 Auth에 연결. RLS로 본인 데이터만 접근.

### [ ] G4 — 서버 진도 동기화
선행: G3, E4 · 완료: 로컬 기록을 로그인 시 계정으로 병합. 충돌은 최신 우선.

### [ ] G5 — 자동 로그아웃 + 비밀번호 힌트
선행: G3 · 완료: 문서의 "2개월 미사용 자동 로그아웃"과 "비밀번호 힌트 단어"(macOS 방식). 힌트는 비밀번호를 복구하지 않고 떠올리게만 한다. 미사용 기간은 `config` 설정값.

### [ ] G6 — 강약점 분석
선행: G4, D5 · 완료: 문항 과목 태그별 정답률 통계. 모델 사용 안 함, 순수 통계.

### [ ] G7 — AI 튜터
선행: G6 · **게이트: 모델 API 키 + 정확성 검토 프로세스 (X6)**
완료: 의학 주제라 오답이 학생에게 그대로 닿는다. 검토 프로세스 없이 켜지 않는다.

### [ ] G8 — 교실(classroom)
선행: G3 · 완료: 반 개설, 참여 코드, 과제 배포·제출 현황.

### [ ] G9 — 후원·프리미엄
선행: A5 · 완료: 가격표·프리미엄 안내·후원 페이지 **UI만, 결제 연결 비움.** 실제 결제는 **게이트: 성인 명의 결제 계정 (X7)**

---

## 관찰
(실행 중 발견했지만 백로그 밖인 것을 한 줄씩)
