# BLOCKERS — 사람이 풀어야 열리는 것

자동 실행이 막힐 때마다 여기에 쌓인다. 여유 있을 때 위에서부터 하나씩 풀면 된다.
자동화는 막힌 항목을 건너뛰고 다른 작업을 계속하므로, **여기 있는 게 밀려도 사이트는 계속 자란다.**

표기: `[ ]` 미해결 · `[x]` 해결 · `[~]` 진행 중

---

## [ ] X2 — Supabase 프로젝트 생성  ★ 곧 필요
**왜:** 2단계(관리 콘솔)의 시작점. 이번 계획에서 콘솔을 초반으로 당겼기 때문에 X1 다음으로 급하다. **이게 풀려야 "코드 안 건드리고 콘텐츠 바꾸기"가 실현된다.**
**할 일:** supabase.com에서 프로젝트 1개 생성 → Settings → API Keys 에서 **Project URL**과 **publishable key**(`sb_publishable_...`) 전달. 무료 티어로 충분.
**참고:** publishable key는 클라이언트에 노출되는 게 정상이다. 방어는 RLS가 하며 B1이 처리한다. (예전 이름 `anon key`는 2026년 말까지 단계적 폐지 중이므로 새 키를 쓴다)
**열리는 것:** B1~B6. 여기까지 오면 시즌·에피소드·팀 명단·발행 주기를 전부 화면에서 만들고 고칠 수 있다.
**예상:** 15분

## [ ] X5 — 강의·문항 원본 내용
**왜:** D2~D5는 코스·강의·문제의 **그릇**까지 자동으로 만들지만 내용은 사람이 쓴다.
**할 일:** 첫 코스 1개분만 있으면 된다 — 단원 3~5개, 강의 5~10개, 문항 20개 정도. 이후는 같은 틀에 부으면 된다.
**열리는 것:** 코스 영역이 잠금 해제 상태로 공개된다.
**예상:** 부원 분담 시 2~3주

## [ ] X4 — 지도교사 승인 + 보호자 동의 양식
**왜:** 이용자 계정과 학습 데이터 서버 저장의 게이트. 이용자 대부분이 미성년자라 기술 문제가 아니라 책임 문제다.
**할 일:** ① G1이 만들어 둘 개인정보처리방침 초안 검토 ② 지도교사 승인 ③ 보호자 동의 양식 ④ 삭제 요청 창구 지정
**열리는 것:** G3 → G4 → G5 → G6.
**급하지 않은 이유:** E 트랙(로그인 없는 진도·북마크·기록)이 이용자 경험을 대신 채운다. E4로 기기 간 이동까지 된다.
**예상:** 절차라 수 주

## [ ] X6 — 모델 API 키 + 정확성 검토 프로세스
**왜:** AI 튜터(G7)의 게이트. 의학·과학 주제라 틀린 답이 학생에게 그대로 닿는다.
**할 일:** ① API 키 ② 답변 범위 제한 규칙(자체 콘텐츠 밖 질문은 거절) ③ 오답 신고 창구 ④ 의료 조언 아님 고지
**열리는 것:** G7. **예상:** X4 이후

## [ ] X7 — 성인 명의 결제 계정
**왜:** 후원·프리미엄의 실제 결제 연결. 결제대행 가입은 보통 성인 명의가 필요하다.
**할 일:** 지도교사·학부모·학교 중 명의 주체를 정하고 정산 책임을 명확히 한다.
**열리는 것:** G9의 결제 연결. **UI는 게이트 없이 먼저 만들어진다.**
**예상:** 미정 — 1년 범위 밖이어도 무방

---

## 블로커가 아닌 것 — 콘솔에서 입력하면 되는 항목

아래는 **B6(콘솔: 사이트 설정)이 끝나면 블로커가 아니게 된다.** 정해지지 않아도 개발이 멈추지 않고, 정해지면 화면에서 입력하면 끝이다. 그때까지는 사이트에 표시되지 않는다.

- **정식 명칭** — 문서에 이름이 셋(BLICS Podcast Club / Aciora Science Media / ACM). 정해질 때까지 자리표시자
- **발행 주기** — 문서의 '월 1회'는 오래된 규칙. 설정값이라 언제든 바꿀 수 있다
- **팀 구성·역할** — 문서 명단은 현재와 다름. 비어 있으면 Who We Are가 빈 상태로 표시된다
- **AP 담당 스태프 자격 규정** — 미확정이라 표시하지 않음. 확정되면 설정에 넣으면 나타난다
- **과목 분류** — 콘텐츠가 정해지는 대로
- **시즌·에피소드 전체** — 확정된 것이 하나도 없다. `IDEAS.md`에 이전 계획의 주제 후보 21개를 보관해 뒀으니 필요할 때 꺼내 쓰면 된다

---

## 해결됨
(풀린 항목을 날짜와 함께 여기로 옮긴다)

### [x] X1 — GitHub 저장소 + 토큰 (2026-09-02)
저장소 `bonogyuyoung/ACM-website` 생성·public·Pages 활성화(`main`/`root`) 확인. `gh api repos/{owner}/{repo}/pages` 결과 `status: built`, 7개 페이지 전부 `https://bonogyuyoung.github.io/ACM-website/` 에서 200 응답. 이 자동 실행 자체가 토큰으로 동작하고 있으므로 PAT 전달도 완료된 상태로 판단.
