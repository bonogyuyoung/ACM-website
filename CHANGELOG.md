# CHANGELOG

자동 실행이 매회 기록한다. 형식: `날짜 · 작업ID · 요약 · 변경 파일`

- 2026-09-20 · F2 · 접근성 기준선. WCAG 4.5:1 대비율을 실제로 계산해(python으로 상대휘도 산출) 미달하는 색을 조정. 문제: 링크/버튼이 같은 `--accent-color`를 공유하는데 흰 텍스트가 올라가는 배경(버튼)은 어두워야 하고 거의 검정에 가까운 다크모드 배경 위 텍스트는 밝아야 해서 한 토큰으로 두 제약을 동시에 만족할 수 없었음 — `--link-color`(페이지 배경 위 텍스트, 라이트/다크 테마별 값)와 `--nav-active-color`(항상 어두운 헤더 위 텍스트, 테마 무관 고정값)를 `--accent-color`(버튼/배지 배경)에서 분리해 색상 테마(blue/green/purple) × 라이트/다크 6가지 조합 전부 4.5:1 이상 확인. 상태 배지 5종 텍스트 대비(흰 텍스트, 기존 2.2~2.9:1로 심각하게 미달)와 `.footer-stage`(3.63:1)도 함께 조정. 키보드 조작(Escape 닫기, 네이티브 `<button>` Enter/Space)·`<main>`/`<nav>`/`<header>`/`<footer>` landmark·이미지 alt(현재 `<img>` 0개)는 기존 구현이 이미 충족해 코드 변경 없음. `a/button/input/[tabindex]:focus-visible`에 보이는 outline 링 추가, 헤더 안에서는 대비를 위해 `--nav-active-color`로 색만 교체. CSS 전용 변경(styles.css 1파일), `node --check`로 JS 미변경 확인 · styles.css, BACKLOG.md

- 2026-09-20 · F1 · 반응형 기반. `styles.css`에 전역 `box-sizing: border-box` 리셋 추가(패딩/보더가 컨테이너 폭을 넘기지 못하게 하는 근본 대책). 실제 360px 폭 가로 스크롤 유발 지점 4곳 수정: `.home-button-grid`가 768px 미디어쿼리에서 `.grid-container`와 함께 1열로 전환되도록 누락돼 있던 오버라이드 추가, `.settings-panel`의 고정 `width: 260px`에 `max-width: calc(100vw - ...)` 상한 추가, `.bookmark-list li`(북마크·대시보드 공용)에 `flex-wrap: wrap`과 `min-width: 0` + `overflow-wrap: anywhere`를 추가해 데이터 기반으로 길이가 정해지지 않는 제목 텍스트가 행을 밀어내지 않게 함, `.header-top`에 `flex-wrap: wrap` 추가. 480px 이하에서 `main`/`header`/`footer` 패딩을 줄이는 미디어쿼리도 신설. CSS 전용 변경(styles.css 1파일), `node --check`로 data.js/script.js 문법 무변경 확인 · styles.css, BACKLOG.md

- 2026-09-13 · E5 · 설정 실동작. 헤더 설정 패널의 정적 "Coming soon" 목록을 실제 컨트롤로 교체 — `renderSettingsControls()`가 `SETTINGS_GROUPS`(Theme: Light/Dark, Color: Blue/Green/Purple, Text Size: A-/A/A+)에서 버튼을 생성하고, `initSettingsControls()`가 클릭을 `updateSetting()`에 연결. 설정은 `localStorage`의 `acm:settings` 하나에 `{theme, colorTheme, fontSize}`로 저장(`getSettings()`가 저장된 값과 기본값을 병합해 부분 저장·손상된 JSON에도 항상 유효한 객체 반환). `applySettings()`가 `<html>`에 `data-theme`/`data-color-theme`/`data-font-size` 속성을 세팅하고 `styles.css`가 그 속성에 대응하는 `:root` 토큰 오버라이드(다크 배경·텍스트·보더 색, 강조색 3종, 글자 크기 87.5%/112.5%)로 실제 적용. `DOMContentLoaded` 맨 앞에서 `applySettings(getSettings())`를 호출해 헤더 렌더 전에 저장된 설정부터 적용. 순수 로직(`getSettings`/`applySettings`/`renderSettingsControls`/`syncSettingsControls`)을 vm 샌드박스로 검증(기본값/부분 저장 병합/손상된 JSON 폴백/알 수 없는 테마 값 폴백/버튼 active·aria-pressed 토글), `node --check` 통과. 구조 변경 파일 2개(script.js, styles.css)라 단일 커밋 · script.js, styles.css, BACKLOG.md
- 2026-09-13 · E4 · 기록 내보내기·가져오기. `dashboard.html`의 "Backup & Restore" 카드에 Export/Import 컨트롤 추가. `getAllLocalData()`가 특정 키를 나열하지 않고 `localStorage`를 순회해 `acm:` 접두사가 붙은 모든 키(북마크·영상 진도·시청 이력·읽은 아티클, 앞으로 추가될 설정·문제 풀이까지)를 하나의 JSON으로 모으고, `exportLocalData()`가 Blob+`<a download>`로 파일 다운로드. `applyImportedData()`는 가져온 JSON에서 `acm:` 접두사가 붙고 값이 문자열인 항목만 `localStorage`에 반영해(그 외 키/타입은 무시) 임의 키 주입을 막는다. Import 후 `renderDashboard()`를 다시 호출해 새로고침 없이 목록 갱신. `initDataTransferControls()`는 해당 요소가 없는 페이지에서는 조용히 아무 것도 하지 않음. Blob/FileReader 등 브라우저 API를 뺀 순수 로직(`getAllLocalData`/`applyImportedData`)을 vm 샌드박스로 검증(정상 내보내기, 왕복 가져오기, `acm:` 아닌 키·문자열이 아닌 값 무시), `node --check` 통과. 구조 변경 파일 2개(script.js, dashboard.html)라 단일 커밋 · script.js, dashboard.html, BACKLOG.md
- 2026-09-13 · E3 · 학습 기록 대시보드. 새 `dashboard.html` + `script.js`의 `renderDashboard()` — 본 영상/읽은 아티클/푼 문제 세 섹션을 전부 로컬 데이터에서 계산해 렌더(빈 섹션은 생략, 전체 0개면 빈 상태). 영상: E1의 재생 위치 키(`acm:videoProgress:*`, 완료 시 삭제됨)와는 별개로 `acm:watchHistory`(youtubeId별 완료 여부, 삭제되지 않음)를 신설해 `onVideoPlayerStateChange()`의 PLAYING/ENDED에서 기록. 아티클: `renderArticleDetail()`이 유효한 아티클을 렌더할 때마다 `acm:readArticles`(id별 제목+읽은 시각)에 기록하는 `markArticleRead()` 추가. 문제: D5(연습문제 엔진)가 아직 없어 아무도 쓰지 않는 `acm:solvedQuestions` 읽기 자리만 미리 만들어 둠(항상 빈 배열 → 섹션 자체가 안 보임, D5가 나중에 같은 키에 쓰면 코드 수정 없이 표시됨). 삭제된 항목을 가리키는 기록은 북마크와 동일하게 "no longer available"로 표시. 헤더 Explore 메뉴에 Dashboard 링크 추가. 구조 변경 파일 2개(script.js, dashboard.html)라 단일 커밋. vm 샌드박스로 빈 상태/영상 진행중+아티클 읽음/영상 완료+삭제된 아티클 세 케이스 모두 렌더 출력 검증, `node --check` 통과 · script.js, dashboard.html, BACKLOG.md
- 2026-09-06 · E2 · 북마크. `localStorage`(`acm:bookmarks`)에 `{type, id, title, addedAt}` 배열로 저장. `type`은 `'item'/'article'/'lesson'`으로 일반화(lesson은 D트랙 미착수라 저장 UI는 없지만 스키마는 미리 대응). `renderBookmarkButton()`을 `renderEpisode()`/`renderArticleDetail()`의 article-header에 추가해 Save/Saved 토글 노출. `renderBookmarks()`가 새 `bookmarks.html`의 타입별 섹션을 렌더(빈 섹션 생략, 전체 0개면 빈 상태, 삭제된 항목은 "no longer available"로 표시 + Remove 버튼). 클릭은 document 위임 리스너 하나로 토글/삭제 모두 처리. 헤더 Explore 메뉴에 Saved 링크 추가. `styles.css`에 `.bookmark-btn`/`.bookmark-list`/`.bookmark-remove-btn` 최소 스타일 추가(기존 `.btn`/`.card` 규약 재사용). 구조 변경 파일이 3개(script.js/bookmarks.html/styles.css)라 2커밋으로 분리(1/2: script.js, 2/2: bookmarks.html+styles.css). vm 샌드박스로 토글 on/off, 목록 렌더(존재/삭제된 항목/빈 상태), 상세 페이지 버튼 포함 여부까지 검증, `node --check` 통과 · script.js, bookmarks.html, styles.css, BACKLOG.md
- 2026-09-06 · E1 · 영상 진도 저장. `renderVideoEmbed()`가 만드는 모든 iframe에 `enablejsapi=1`과 고유 id 부여, `initVideoProgress()`가 YouTube IFrame Player API를 붙여 재생 위치를 `localStorage`(`acm:videoProgress:<youtubeId>`)에 5초 간격/일시정지 시 저장. 복귀 시 저장된 위치(끝나기 5초 전보다 이전일 때만)로 자동 이동해 이어보기, 끝까지 본 영상은 `ENDED`에서 기록 삭제. localStorage 불가 환경에서도 조용히 무시. YouTube API 스크립트는 페이지당 한 번만 로드, 기존 `onYouTubeIframeAPIReady` 콜백 체이닝. vm 샌드박스로 embed 마크업·progress 저장/조회/삭제·seek 조건·상태 분기·iframe 0개 케이스까지 검증, `node --check` 통과. 실제 브라우저 재생 동작은 정적 검증 범위 밖 · script.js, BACKLOG.md
- 2026-09-06 · C6 · future.html 3부 구조. `data.js`의 `roadmap`(June Prototype/Summer Archive/Future Platform — 폐기된 옛 일정 문구, BACKLOG 관찰 항목에 지적됨)을 `futurePlatform` 3부(Science Communication/Academic Courses/Personalized Learning)로 교체. 3부(Personalized Learning)는 `gated: true` + `gateNote` 필드로 게이트 대기 상태를 명시(X4: 지도교사 승인 + 보호자 동의). `script.js`의 `renderRoadmap()`을 `renderFuturePlatform()`으로 교체해 새 배열을 렌더, gated 항목만 "Status: Gated — ..." 문구 추가. `future.html`에서 "June prototype" 언급 등 옛 문구 제거. 구조 변경 파일이 3개(data.js/script.js/future.html)라 2커밋으로 분리(1/2: data.js+script.js, 2/2: future.html). 0개/3개 케이스 모두 vm 샌드박스로 렌더 출력 검증, `node --check`로 문법 확인 · data.js, script.js, future.html, BACKLOG.md
- 2026-09-02 · C4 · 아카이브. `archive.html` 신설(기존 7개 파일명은 그대로 유지, 새 페이지 추가) + `script.js`에 `renderArchive()` 추가 — 상태가 `config.statusStages` 마지막 단계이고 `item.publishedAt`(새 선택 필드, final stage 도달 시점)이 있는 항목만 월별로 그룹핑해 최신순 카드로 렌더. `config.publishCadence`가 숫자면 "Target: N · Actual: M", 아니면(현재처럼 `null`) "Published: M"만 표시해 고정 목표값을 가정하지 않음. 헤더 Explore 메뉴에 Archive 링크 추가. 0개/월 2개 혼합(publishedAt 없는 published 항목은 제외됨)/cadence 지정 세 케이스 모두 vm 샌드박스로 렌더 출력 검증, `node --check`로 문법 확인. 새로 도입한 `publishedAt`/`publishCadence` 형태를 BACKLOG.md `## 관찰`에 기록 · archive.html, script.js, BACKLOG.md
- 2026-09-02 · A10 · 홈 재구성. `script.js`에 `renderSiteStatus()` 추가 — `collections.length`/`allItems.length`와 `config.statusStages`의 마지막 단계 기준 published 개수를 계산해 "N Seasons · N Episodes (N Published)" 형태로 `index.html`의 새 `#site-status` 컨테이너에 렌더. 하드코딩된 숫자 리터럴 없음(전부 `.length`/필터 계산). 0개 시즌이거나 시즌은 있지만 항목이 0개인 두 경우 모두 기존 `renderEmptyState()`를 재사용해 "아직 준비 중" 문구로 자연스럽게 표시. 새 CSS 클래스 없이 기존 `.card-meta`/`.empty-state` 규약만 재사용해 `styles.css`는 건드리지 않음. 0개/2묶음-4항목-2published 세 케이스 모두 vm 샌드박스로 렌더 출력 검증 · index.html, script.js
- 2026-09-02 · A9 · 영상 임베드. `script.js`에 `renderVideoEmbed(video)` 추가 — `video.youtubeId`가 있으면 16:9 반응형 `youtube-nocookie.com` iframe, 없으면 "Coming Soon" 배지를 같은 크기 박스로 렌더(레이아웃 유지). `episode.html`의 Video 슬롯과 `videos.html` 카드가 이 함수를 씀. `featuredVideo`는 `data.js`에서 하드코딩 대신 `allItems` 중 상태가 `config.statusStages`의 마지막 단계이고 video 슬롯이 있는 항목을 `video.lastUpdated` 기준 최신순으로 자동 선택하도록 변경(1/2 커밋) — 해당 항목이 없으면 `null`이라 `renderFeaturedVideo()`가 섹션 자체를 숨김. 0개/여러 항목/youtubeId 없는 published 항목 세 케이스 모두 vm 샌드박스로 데이터 파생 검증, `renderVideoEmbed()`는 fake DOM으로 세 분기(id 있음/슬롯 없음/슬롯은 있지만 id 없음) 출력 확인 · script.js, styles.css (+ data.js, 이전 커밋)
- 2026-09-02 · A8 · 아티클 상세. `article.html?id=`과 `script.js`의 `renderArticleDetail()` 추가 — `article-template.html`의 7섹션(요약·왜 중요한가·핵심 용어·본문·단계별 설명·면책·출처)을 `item.article` 필드로 승격해 렌더. 면책 문구는 기존 문구 그대로 고정 텍스트로 유지(항목별 데이터 아님), 본문 필드는 항상 비워 둠. 빈 필드는 전부 "Coming soon."으로 표시되고 레이아웃 유지. `data.js`의 아티클 링크는 이제 `article.html?id=<item id>`로 자동 생성(1/2 커밋). 없음/빈 필드/완전한 데이터 세 케이스 모두 시뮬레이션으로 검증 · article.html, script.js (+ data.js, 이전 커밋)
- 2026-09-02 · A7 · 항목 상세 페이지. `episode.html?id=`과 `script.js`의 `renderEpisode()`/`findItemById()` 추가 — 핵심 개념 목록, 큰 질문, 상태 배지, 연결된 아티클/영상/논문 자리(없으면 Coming Soon)를 렌더. 코어 개념·큰 질문이 비어 있으면 "Coming soon."으로 표시하며 레이아웃 유지. id가 없거나 못 찾으면 `seasons.html`로 안내하는 화면을 보여줌(자동 리다이렉트 아님). 정상/빈 필드/잘못된 id 세 케이스 모두 시뮬레이션으로 검증 · episode.html, script.js
- 2026-09-02 · A6 · 묶음 목록 페이지. `seasons.html` 추가 + `script.js`에 `renderCollections()` 추가(제목·부제·항목 수·진행률, 진행률은 `config.statusStages`의 마지막 단계 기준으로 계산해 특정 상태명을 하드코딩하지 않음). 0개면 A4의 빈 상태 재사용. 헤더 Explore 메뉴에 `config.labels.collectionPlural`로 링크 추가(모든 페이지에 자동 반영). 0개·N개 케이스 모두 시뮬레이션으로 검증 · seasons.html, script.js
- 2026-09-02 · A5 · 디자인 토큰 + 컴포넌트 규약. `styles.css` 전체를 훑어 색·타이포·간격·모서리 반경·그림자 리터럴을 `:root` 스케일(`--text-*`, `--space-*`, `--radius-*`, `--shadow-*`)로 교체. README에 토큰 스케일과 카드/배지/버튼/빈상태 클래스 규약 표 추가 · styles.css, README.md
- 2026-09-02 · A4 · 빈 상태 시스템. `renderEmptyState()` 공통 함수를 추가하고 topics/articles/videos/team-roles/roadmap/home-buttons 렌더 함수 전부가 0개일 때 이걸 쓰도록 통일. `styles.css`에 `.empty-state` 스타일 추가(grid 안에서는 전체 폭 차지). 콘텐츠 0개인 topics/articles/videos 페이지가 깨진 화면이 아니라 안내 문구가 있는 완성된 화면으로 보이는 것을 시뮬레이션으로 확인 · script.js, styles.css
- 2026-09-02 · A3 · 상태 파이프라인. `getBadgeClass()`가 `config.statusStages`에 없는 상태를 받으면 `badge default`로 떨어지도록 수정(기존엔 배경색 없는 배지가 나오는 버그였음). `styles.css`에 5단계(planned/researching/drafting/review/published) 배지 색을 전부 정의하고, 더 이상 쓰이지 않는 `script-writing` 배지 제거 · script.js, styles.css
- 2026-09-02 · A2 · 빈 계층 스키마 + 설정 분리. `data.js`를 `config`(사이트명·표시 명칭·상태 단계 목록·과목 분류·발행 주기)와 `collections`(묶음→항목 2계층, 빈 배열)로 재구성. 기존 Antibiotic Resistance/Vaccines/CRISPR 3건과 `featuredVideo`의 하드코딩된 콘텐츠 제거. `topics`/`articles`/`videos`는 `collections`에서 map/filter로 파생시켜 `script.js` 무수정으로 7개 페이지 정상 동작 확인(0개·N개 양쪽 케이스 검증) · data.js
- 2026-09-02 · A1 · 저장소 구조 정리 + 배포 확인. `.nojekyll` 추가로 Pages가 파일을 그대로 서빙하도록 지정, README에 폴더 구조와 편집 안내 작성. Pages API로 7개 페이지 전부 200 응답 확인, 상대경로 링크(styles.css/data.js/script.js) 정상. X1(저장소+토큰)이 이미 풀려 있음을 확인해 BLOCKERS.md 해결됨으로 이동 · README.md, .nojekyll, BLOCKERS.md
- 2026-09-01 · v3 · **콘텐츠를 코드에서 분리.** 문서의 5시즌 21편·팀 명단·월 1회 발행 규칙·AP 자격 규정이 전부 오래된 것으로 확인됨 → 시딩을 제거하고 빈 스키마 + 설정값 구조로 전환. 관리 콘솔을 2단계로 앞당겨 이후 콘텐츠 변경이 저장소를 건드리지 않게 함. 21편은 `IDEAS.md`에 후보로 보관. 4단계 49개 작업 · PLAN.md, BACKLOG.md, BLOCKERS.md, IDEAS.md, EPISODES.md(폐기)
- 2026-09-01 · v2 · 목표를 base website에서 full website 1년 완성으로 변경. 보류했던 계정·코스·AI 튜터·스태프 콘솔·수익화를 범위로 복원, 트랙 + 게이트 모델 도입 · PLAN.md, BACKLOG.md, BLOCKERS.md
- 2026-09-01 · v1 · 최초 계획 수립 · PLAN.md, BACKLOG.md, EPISODES.md
