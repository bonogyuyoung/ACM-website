# ACM 웹사이트 — Base에서 Full로, 1년 계획

기준 코드: `Base Website/` (정적 HTML 7 + data.js + script.js + styles.css)
목표: **문서에 있는 기능을 하나도 빼지 않은 full website를 1년 안에**
개정: 2026-09-01 (v3 — 콘텐츠를 코드에서 분리)

> 참고 문서 `BLICS Aciora Science Media ASM.pdf`는 **기능 요구사항의 출처**로만 쓴다.
> 그 안의 콘텐츠 계획(5시즌 21편), 팀 명단, 월 1회 발행 규칙, AP 자격 규정은 **전부 오래된 것이며 현재 확정된 바 없다.**
> 21편 목록은 버리지 않고 `IDEAS.md`(주제 후보 창고)에 보관했다. 코드도 자동화도 이것을 읽지 않는다.

---

## 0. 이 계획의 작동 원리

**문제 1 — 사람 의존.** full website에는 지도교사 승인, 보호자 동의, 성인 명의 결제처럼 사람만 할 수 있는 일이 섞여 있다. 하나 걸리면 계획 전체가 선다.

**문제 2 — 콘텐츠 유동성.** 시즌도 에피소드도 팀 명단도 발행 주기도 아직 정해지지 않았고 앞으로 계속 바뀐다. 이걸 코드에 박으면 **내용이 바뀔 때마다 저장소를 건드려야 한다.**

**수정.**
- 문제 1 → 작업마다 선행 조건을 붙이고, 자동 실행이 **막힌 작업을 건너뛰고 다음으로 넘어가게** 한다. 막힌 것은 `BLOCKERS.md`에 "사람이 무엇을 해야 풀리는지"와 함께 쌓인다.
- 문제 2 → **코드에 콘텐츠를 두지 않는다.** 사이트는 콘텐츠 0개 상태에서 정상 동작하는 껍데기로 만들고, 내용은 관리 콘솔에서 정의한다. 콘솔을 초반에 올리는 것이 이 계획의 최우선 순서다.

**효과.** 사용자가 쓰는 시간은 두 가지뿐이다 — `BLOCKERS.md`를 여유 있을 때 하나씩 푸는 것, 그리고 콘솔에서 내용을 채우는 것. 콘텐츠가 몇 번을 바뀌든 저장소도 코드도 건드리지 않는다.

---

## 1. 확정 결정 (2026-09-01)

| 항목 | 결정 | 의미 |
|---|---|---|
| **콘텐츠** | **코드에 고정하지 않음** | 시즌·에피소드·팀·발행주기·과목 분류 전부 콘솔에서 정의. 코드에는 어떤 제목도 박히지 않는다 |
| **관리 콘솔** | **초반에 바로** | 기본 페이지가 뜨자마자 Supabase + 콘솔. 그 뒤로는 화면에서 만들고 지운다 |
| 아키텍처 | 정적 + Supabase | GitHub Pages 유지. 서버 상태가 필요한 것만 Supabase JS |
| 실행 강도 | 주 2회 · 막힐 때까지 연속 | 1회 실행이 작업 1개에서 멈추지 않음 |
| 계정·개인정보 | 로그인 없는 진도저장부터 | 1단계는 브라우저 저장만. 계정은 게이트 뒤 |
| 수익화 | UI만 만들고 비활성 | 결제 연결은 성인 명의 확보 후 |
| 자동화 범위 | 기능까지. **본문은 사람** | 아티클 본문·영상 스크립트·강의 내용·문항 |
| 정식 명칭 | 미정 → 자리표시자 | 확정되면 설정값 하나로 반영 |

---

## 2. 콘텐츠 비고정 원칙

이게 v3의 핵심이다. 아래를 어기면 나중에 반드시 되돌리는 일이 생긴다.

1. **코드에 콘텐츠 문자열을 쓰지 않는다.** 에피소드 제목, 시즌 이름, 팀원 이름, 과목명 — 전부 데이터다. 예시 데이터조차 `sample` 표시 없이 넣지 않는다.
2. **개수를 가정하지 않는다.** 시즌 5개, 에피소드 4~5개, 과목 5개 같은 숫자를 레이아웃이나 로직이 전제하지 않는다. 0개도 100개도 돌아가야 한다.
3. **빈 상태가 정상 상태다.** 콘텐츠가 하나도 없을 때 페이지가 깨지지 않고 "아직 없습니다"를 제대로 보여줘야 한다. 이건 예외 처리가 아니라 기본 화면이다.
4. **명칭은 설정값이다.** '시즌'을 '시리즈'로, '에피소드'를 '회차'로 바꾸는 데 코드 수정이 필요하면 안 된다. 발행 주기도 숫자 설정값이다.
5. **구조는 2계층으로 고정.** `묶음 → 항목` (기본 표기: 시즌 → 에피소드). 각 항목은 아티클·영상·논문 슬롯을 선택적으로 갖는다. 이 이상은 추상화하지 않는다 — 무한히 유연한 스키마는 아무도 못 쓴다.

---

## 3. 아키텍처 — 무엇이 언제 서버로 가는가

개인정보가 **없는 것부터** 순서대로 옮긴다.

```
1단계  data.js               빈 스키마 + 설정. 진도·설정은 localStorage.        개인정보 0
2단계  Supabase DB + 콘솔    ★ 여기서 콘텐츠가 유동적이 된다                   개인정보 0
3단계  스태프 로그인          동아리 부원만 로그인 → 콘솔 접근                   부원 이메일뿐
──────────────────── 게이트: 지도교사 승인 + 보호자 동의 양식 ────────────────────
4단계  이용자 계정            서버 진도 동기화, 자동 로그아웃                    이용자 개인정보
5단계  개인화                 강약점 분석 → AI 튜터                             학습 데이터
```

**2단계가 게이트보다 한참 앞에 있는 게 설계의 핵심이다.** 콘텐츠 관리는 이용자 개인정보를 하나도 안 건드리므로, 승인 절차와 무관하게 먼저 켤 수 있다.

---

## 4. 실행 4단계

| 단계 | 내용 | 끝나면 |
|---|---|---|
| **1 · 껍데기** | 저장소·배포, 빈 스키마, 상태 파이프라인, 디자인 토큰, 목록·상세 페이지 | 콘텐츠 0개인 사이트가 정상 동작 |
| **2 · 콘솔** ★ | Supabase 연결·RLS, 콘텐츠 DB, 관리 콘솔, 스태프 로그인 | **코드를 안 건드리고 내용을 만들고 지울 수 있다** |
| **3 · 기능** | 검색·필터, 논문, 아카이브, 코스, 연습문제, 진도·북마크·설정, 반응형·접근성·SEO | 문서의 기능 요구가 대부분 충족 |
| **4 · 게이트 뒤** | 이용자 계정, 서버 동기화, 강약점 분석, AI 튜터, 교실, 결제 | 승인·키·명의가 풀리는 대로 |

2단계까지가 이 계획에서 가장 중요하다. 여기만 지나면 콘텐츠 계획이 몇 번을 바뀌어도 개발 일정에 영향이 없다.

---

## 5. 일정

전체 49개 작업, 주 2회 실행, 1회에 1~3개 → 순수 진행 3~4개월. 나머지가 막힘·재작업 완충이다.

**콘텐츠 속도는 이제 사이트 완성과 분리됐다.** 이전 판에는 "21편을 월 1회로 내면 21개월"이라는 산술 문제가 있었지만, 확정된 편수가 없으므로 그 계산 자체가 성립하지 않는다. 사이트는 콘텐츠 0개로 완성될 수 있고, 이후 채우는 속도는 동아리가 정하면 된다. 발행 주기도 코드가 아니라 설정값이다.

---

## 6. 절대 건드리지 않는 것 (frozen)

1. 빌드 도구·프레임워크·번들러 도입 금지. 재검토 기준은 §7에 수치로.
2. 기존 HTML 7개 파일명 유지 (`index / topics / articles / videos / future / about / contact`).
3. 공통 UI 복붙 금지 — `script.js`의 `#header-container` 주입 방식이 이 코드베이스의 컴포넌트 규약이다.
4. **RLS 켜기 전에 Supabase 테이블 생성 금지.** publishable 키는 클라이언트에 노출되는 게 정상이지만 RLS가 꺼져 있으면 그 키로 테이블 전체가 읽고 써진다. Supabase에서 사고가 가장 많이 나는 지점이다.
5. 게이트 통과 전 개인정보 서버 저장 금지 — 이메일·이름·점수·학습기록 전부.
6. **콘텐츠 하드코딩 금지** (§2).
7. 본문·강의·문항 내용 작성 금지. 그릇과 빈 필드까지만.
8. 한 커밋에서 구조 변경 파일 3개 이상 금지.

---

## 7. 정적 유지를 재검토할 기준

아래 중 **둘 이상**이 동시에 참이면 그 회차에서 멈추고 프레임워크 이전을 재검토한다.

- HTML 페이지 수 **25개** 초과 · 같은 UI 조각이 **3곳 이상**에서 벌어짐 · `script.js` **1,500줄** 초과 · 로그인 상태로 화면이 갈리는 페이지 **8개** 초과

현재: 페이지 7개, `script.js` 약 330줄.

---

## 8. 실행 방식

**1회 실행:** clone → `BACKLOG.md`·`BLOCKERS.md` 확인 → 선행 조건이 풀린 첫 작업 → 구현 → 7개 페이지 콘솔 에러 0 → 커밋 → **막히면 멈추지 않고** `BLOCKERS.md`에 적고 다음 작업으로 → 가능한 작업이 없어질 때까지 반복 → `CHANGELOG.md` 기록.

**설정 (사람이 한 번만):**
1. GitHub 저장소 생성 → `Base Website` 파일들을 루트에 업로드 → Pages 활성화
2. 저장소 하나로 범위를 좁힌 fine-grained PAT (Contents = Read and write)
3. 저장소 주소·토큰 전달 → 주 2회 예약 작업 생성
4. **Supabase 프로젝트 1개 생성** — 2단계가 초반이라 곧 필요하다. 무료 티어
